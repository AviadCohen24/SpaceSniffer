import { styled } from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  width: 100%;
  height: calc(100vh - 70px);
  align-items: center;
  justify-content: center;
  align-items: center;
`;

export const EmptyString = styled.h1`
  font-size: 20px;
  font-family: Verdana, Geneva, Tahoma, sans-serif;
  margin: 0;
  color: gray;
  opacity: 0.8;
`;
